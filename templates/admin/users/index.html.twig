<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Users · Admin · aavion Studio</title>
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; margin: 0; padding: 2rem; background: #f5f7fb; color: #111827; }
        h1 { margin-top: 0; }
        .card { background: #fff; padding: 1.75rem; border-radius: 12px; box-shadow: 0 18px 36px rgba(15,23,42,.08); margin-bottom: 1.5rem; }
        form.filter { display: flex; gap: 1rem; flex-wrap: wrap; align-items: flex-end; }
        label { display: block; font-weight: 600; margin-bottom: .35rem; }
        input[type="text"], select { padding: .65rem .9rem; border-radius: 10px; border: 1px solid #cbd5f5; background: #fff; min-width: 220px; }
        button.primary { padding: .7rem 1.4rem; border-radius: 10px; border: none; background: linear-gradient(135deg, #2563eb, #1d4ed8); color: #fff; font-weight: 600; cursor: pointer; }
        table { width: 100%; border-collapse: collapse; }
        th, td { text-align: left; padding: .75rem; border-bottom: 1px solid #e5e7eb; }
        .tag { display: inline-block; padding: .25rem .55rem; border-radius: 999px; font-size: .75rem; background: #e0f2fe; color: #05558a; text-transform: uppercase; letter-spacing: .04em; }
        a.link { color: #2563eb; text-decoration: none; font-weight: 600; }
        .flash-success { margin-bottom: 1rem; padding: .75rem 1rem; border-radius: 10px; background: rgba(34,197,94,.1); color: #15803d; }
        .flash-error { margin-bottom: 1rem; padding: .75rem 1rem; border-radius: 10px; background: rgba(239,68,68,.1); color: #b91c1c; }
        .empty { padding: 2rem; text-align: center; color: #6b7280; }
    </style>
</head>
<body>
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="flash-{{ label }}">{{ message }}</div>
        {% endfor %}
    {% endfor %}

    <h1>User management</h1>

    <div class="card">
        <form class="filter" method="get" action="{{ path('admin_users_index') }}">
            <div>
                <label for="q">Search</label>
                <input type="text" id="q" name="q" value="{{ query }}" placeholder="Email or name">
            </div>
            <div>
                <label for="status">Status</label>
                <select id="status" name="status">
                    <option value="">All statuses</option>
                    {% set statuses = {
                        'active': 'Active',
                        'pending': 'Pending',
                        'disabled': 'Disabled',
                        'archived': 'Archived'
                    } %}
                    {% for key, labelText in statuses %}
                        <option value="{{ key }}" {% if status == key %}selected{% endif %}>{{ labelText }}</option>
                    {% endfor %}
                </select>
            </div>
            <div>
                <button class="primary" type="submit">Apply filters</button>
            </div>
        </form>
    </div>

    <div class="card">
        {% if users is empty %}
            <div class="empty">No users found for the selected filters.</div>
        {% else %}
            <table>
                <thead>
                    <tr>
                        <th>Email</th>
                        <th>Name</th>
                        <th>Status</th>
                        <th>Roles</th>
                        <th>Last login</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    {% for user in users %}
                        <tr>
                            <td>{{ user.email }}</td>
                            <td>{{ user.display_name }}</td>
                            <td><span class="tag">{{ user.status }}</span></td>
                            <td>
                                {% if user.roles is empty %}
                                    <span class="tag" style="background:#f5f3ff;color:#4c1d95;">ROLE_VIEWER</span>
                                {% else %}
                                    {% for role in user.roles %}
                                        <span class="tag" style="background:#eaeffd;color:#3730a3;">{{ role }}</span>
                                    {% endfor %}
                                {% endif %}
                            </td>
                            <td>
                                {% if user.last_login_at %}
                                    {{ user.last_login_at|date('Y-m-d H:i') }}
                                {% else %}
                                    —
                                {% endif %}
                            </td>
                            <td><a class="link" href="{{ path('admin_users_edit', { id: user.id }) }}">Edit</a></td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        {% endif %}
    </div>

    <p><a class="link" href="{{ path('admin_user_invitations') }}">Manage invitations</a></p>
</body>
</html>
