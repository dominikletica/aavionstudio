# This file overrides environment variables when APP_ENV=test.
# Tests use isolated SQLite databases and installer artefacts under var/test/.

APP_ENV=test
DATABASE_URL="sqlite:///%kernel.project_dir%/var/test/system.brain"
