[
  {
    "section": "setup.environment",
    "type": "inline_help",
    "title": "Choose the right environment",
    "body": "Use <code>prod</code> for live deployments so caches and logging run in production mode. Keep <code>dev</code> for local/staging instances where debug output is acceptable."
  },
  {
    "section": "setup.environment",
    "type": "tooltip",
    "title": "Generate a strong secret",
    "body": "If you leave the secret empty the installer will generate one automatically. Copy it to your password manager so you can rotate it later via <code>bin/init</code>.",
    "target": "environment.secret"
  },
  {
    "section": "setup.storage",
    "type": "inline_help",
    "title": "Plan storage ahead",
    "body": "Point the storage root at a filesystem with regular backups. The installer will create <code>databases/</code>, <code>uploads/</code>, <code>snapshots/</code>, and <code>backups/</code> folders underneath."
  },
  {
    "section": "setup.storage",
    "type": "tooltip",
    "title": "Writable directories",
    "body": "The web user must be able to create directories under the chosen storage root. Double-check permissions with <code>ls -la</code> if the diagnostics step still shows warnings.",
    "target": "storage.root"
  },
  {
    "section": "setup.admin",
    "type": "inline_help",
    "title": "Secure the first administrator",
    "body": "Pick a strong password that follows the configured policy and enable MFA if possible. Recovery contacts help regain access if MFA devices are lost."
  },
  {
    "section": "setup.admin",
    "type": "tooltip",
    "title": "Password policy",
    "body": "The installer enforces the policy defined in <code>core.users.password_policy</code>. Use a passphrase that includes numbers, mixed case, and special characters for best results.",
    "target": "admin.password"
  },
  {
    "section": "setup.diagnostics",
    "type": "inline_help",
    "title": "Troubleshoot missing extensions",
    "body": "Missing PHP extensions can often be installed via your package manager (e.g. <code>apt install php-intl</code>). Re-run diagnostics after installing to confirm they are loaded."
  },
  {
    "section": "setup.diagnostics",
    "type": "tooltip",
    "title": "Filesystem check",
    "body": "If a directory is marked as not writable, create it manually and run <code>chmod</code>/<code>chown</code> so PHP can write caches and database files."
  },
  {
    "section": "setup.summary",
    "type": "inline_help",
    "title": "Review before finalising",
    "body": "Double-check that email addresses, base URL, and storage root are correct. The installer will lock itself after completion; use the summary as your final checklist."
  },
  {
    "section": "setup.summary",
    "type": "tooltip",
    "title": "Download configuration",
    "body": "After finalisation, copy the contents of <code>.env.local</code> and store them securely (secrets manager or vault). This makes rotating environments easier later.",
    "target": "summary.finalize"
  }
]
